"""Vendored gRPC stubs for the Hyperliquid node L1 gateway (provider-neutral).

Generated from ``hyperliquid_l1_gateway.proto`` — see ``README.md`` for the
upstream source and the exact regeneration command. These modules are imported
lazily (only when the gRPC adapter actually connects) and require the optional
``grpc`` extra; nothing here is imported during normal CI / default runtime.
"""
